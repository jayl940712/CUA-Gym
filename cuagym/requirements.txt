-e nemo-gym[dev] @ ../../
Pillow
# Pin playwright to the same version as resources_servers/webarena so both
# server venvs resolve the same Chromium revision from the shared cache.
playwright==1.62.0
pyautogui
# Keep the Xlib distribution expected by pyautogui explicit and deterministic.
# Do not add python-xlib: both distributions install the same Xlib/* files.
python3-xlib==0.15
# Bundle setup/reward scripts import requests; they run with this venv's python.
requests
